/*
    The Static memory is the memory space that is created for each variable. this memory space
    can not be changed or relased manually in execution
*/
#include <stdio.h>

int main()
{
    int Static[10]; // A static memory is declared
}